export class Customer {
 
	constructor(public loanId:number, public firstName:string, public lastName:string, public email:string, public mobile:number, public salary:number, public address:string, public adhar:Number) {
	}
}
    